if have_tensorflow_lite
  install_data('gst-launch-object-detection-yolov5-tflite.sh',
    install_dir: examples_install_dir
)
endif

if have_pytorch
  install_data('gst-launch-object-detection-yolov5-torchscript.sh',
    install_dir: examples_install_dir
  )
endif
